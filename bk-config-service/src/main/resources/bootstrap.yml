spring:
  application:
    name: bk-config-service
    
  profiles: 
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: "*"
    enabled-by-default: true

  metrics:
    tags:
      application: ${spring.application.name}
      environment: ${spring.profiles}    